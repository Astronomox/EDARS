# analytics/requirements-dev.txt
# Install with: pip install -r requirements-dev.txt
#
# These are development/testing dependencies only.
# Production requirements stay in requirements.txt

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-mock>=3.12.0
httpx>=0.27.0              # async test client for FastAPI

# Linting & formatting (all free, open source)
ruff>=0.4.0                # replaces flake8 + isort + pyupgrade
mypy>=1.9.0                # static type checking

# Type stubs
types-redis>=4.6.0
